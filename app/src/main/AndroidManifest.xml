<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mx.com.charlyescaz.colaboradorestest">

    <application
        android:name=".ColaboradoresApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ColaboradoresTest">
        <activity android:name=".ui.launch.view.LaunchActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.home.view.HomeActivity" />
        <activity android:name=".ui.auth.view.LoginActivity" />
        <activity android:name=".ui.collaborators.list.view.CollaboratorsActivity" />
        <activity android:name=".ui.collaborators.map.view.MapActivity" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyA72qzym7DUL1klolQ4DFF_Ok0UegzhLpk" />

    </application>

</manifest>